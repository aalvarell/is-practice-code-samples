# solo se importa lo que hayamos declarado,
# a nivel de paquete, que se tiene que importar
from mates.calculo import *

print(derivadas.derivar('x^3'))
print(integrales.integrar('x^3'))
print(test_variable)